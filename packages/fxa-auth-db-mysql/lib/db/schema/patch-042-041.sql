-- Rollback; commented out to prevent accidental running
-- in production.

-- DROP PROCEDURE `deviceFromTokenVerificationId_1`;

-- UPDATE dbMetadata SET value = '41' WHERE name = 'schema-patch-level';

