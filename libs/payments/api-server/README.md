# payments-api-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build payments-api-server` to build the library.

## Running unit tests

Run `nx run payments-api-server:test-unit` to execute the unit tests via [Jest](https://jestjs.io).

## Running integration tests

Run `nx run payments-api-server:test-integration` to execute the integration tests via [Jest](https://jestjs.io).
