# shared-glean

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-glean` to build the library.

## Running unit tests

Run `nx test shared-glean` to execute the unit tests via [Jest](https://jestjs.io).
