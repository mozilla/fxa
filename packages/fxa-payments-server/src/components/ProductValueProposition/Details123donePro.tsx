import React from 'react';
import { Plan } from '../../store/types';

type Details123doneProProps = { plan: Plan };

const Details123donePro = ({
  plan
}: Details123doneProProps) => (
  <p>Amazing 123done Pro features</p>
);

export default Details123donePro;
