#!/usr/bin/env sh

# Load nvm if available (for nvm users)
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# Run lint-staged (works with nvm or system Node)
npx lint-staged
