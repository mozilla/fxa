import { Meta } from '@storybook/react';
import Footer from '.';

export default {
  title: 'components/Footer',
  component: Footer,
} as Meta;

export const Default = () => {
  return <Footer />;
};
