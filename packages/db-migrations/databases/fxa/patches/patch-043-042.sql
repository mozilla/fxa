-- DROP PROCEDURE `prune_2`;
-- -- no need for this 'prune-last-ran'
-- DELETE FROM dbMetadata WHERE name = 'prune-last-ran';

-- UPDATE dbMetadata SET value = '42' WHERE name = 'schema-patch-level';
